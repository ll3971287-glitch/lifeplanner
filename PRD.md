# PRD：Ultimate Life System —— 个人任务 & 项目进度管理（本地 Web 版）

> 版本 v1.0 · 第一版范围 · 后续开发依据
> 项目位置：`~/workspace/lifeplanner/`（工程目录沿用初始名 lifeplanner；产品定名 Ultimate Life System，简称 ULS）

## 1. 产品目标与使用场景

**目标**：做一款运行在安卓平板本地浏览器里的轻量个人任务&项目进度管理网站（PWA），把「待办清单、项目进度、专注计时、习惯打卡、复盘、日历、标签」七件事收进一个工具，数据互相联动、全本地留存，打开即用、无登录无云端。

**解决什么问题**：个人学习/工作常常要同时在好几个 App 里管理任务、项目进度、专注时长、打卡和复盘，数据互相不通、切换麻烦。本产品统一承载，并让任务完成、专注记录、日历、首页摘要相互联动，形成「计划 → 执行 → 记录 → 复盘」闭环。

**使用场景**：
- 学生 / 自我管理者：学期课程项目（学习类项目）拆解跟踪；日常待办 + 番茄专注学习。
- 上班族 / 自由职业：工作任务（工作类项目）拆解、进度与截止管理；日/周复盘。
- 习惯养成：每日打卡（喝水、阅读、运动等），靠热力图/折线/甘特图看到长期趋势。
- 时间管理：日/周/月日历统览安排与专注分布；周期复盘沉淀改进。

**运行环境**：安卓平板现代浏览器（支持 IndexedDB 与 PWA），竖/横屏自适应；数据只存本设备，刷新、关闭、重启均不丢失。第一版不做登录、不做云。

## 2. 整体页面框架与导航结构

- **全局框架**：顶栏（左侧当前模块名；右侧全局入口图标：标签、复盘、设置）+ 内容区 + 底部导航。
- **底部主导航（5 Tab）**：今日 / 待办 / 项目 / 日历 / 打卡。
- **专注双形态**：① 任意任务/子任务旁的「专注」按钮 → 全屏沉浸计时浮层（可收起、后台计时不中断）；② 独立专注页 `/focus`（浮层右上角与首页卡片可进入），二者共用同一计时核心。
- **路由清单**：

| 路由 | 页面 | 入口 |
|---|---|---|
| `/` | 今日首页 | 底部 Tab 1 |
| `/todos` | 待办清单 | 底部 Tab 2 |
| `/projects` | 项目总览 | 底部 Tab 3 |
| `/projects/:id` | 项目详情（网格） | 总览卡片点击 |
| `/calendar` | 日历 日/周/月 | 底部 Tab 4 |
| `/checkins` | 打卡列表 | 底部 Tab 5 |
| `/checkins/:id` | 打卡详情（图表） | 打卡卡片点击 |
| `/tags` | 标签专题 | 顶栏 |
| `/tags/:id` | 标签详情 | 标签卡片点击 |
| `/reviews` | 复盘 | 顶栏 |
| `/focus` | 专注页 | 顶栏/首页卡片/浮层右上角 |
| `/settings` | 设置 | 顶栏 |

## 3. 首页内容结构（今日摘要中枢）

自上而下卡片区（平板双列自适应、窄屏单列，各卡片可点击跳转）：
1. **今日待办**：今天到期的时间点/时间段任务 + 已开启「放入首页」的项目子任务；行内直接勾选完成。
2. **项目进度**：未完成项目前 3 个，进度条 + 截止日倒计时；点击进项目详情。
3. **今日专注**：大按钮「开始专注」（可不选任务直接计时）+ 今日累计会话数与时长；计时进行中显示迷你状态并可一键回到计时界面。
4. **今日打卡**：每个打卡项目一行「X/Y 达标」，行内快捷打卡按钮。
5. **即将到期**：未来 48h 到期的任务与项目提醒（逾期任务红字置顶提醒）。
6. 底部小入口：标签、复盘（当日/本周未写时显示提示）。

顶部问候：日期 +「今日完成 X 件事」概览。

## 4. 各模块用途

| 模块 | 用途 |
|---|---|
| 待办 Todo | 日常任务的记录、多级拆分与完成跟踪；可设时间点/时间段 |
| 项目 | 学习/工作类长周期任务的拆解（最小子任务）、工作量与截止管理、进度可视化（网格） |
| 专注 | 番茄钟与自由正计时；为任务/项目子任务提供沉浸计时并沉淀专注记录 |
| 标签 | 全站统一分类体系：任务、项目、项目子任务可打标签；标签页聚合查看与跳转 |
| 日历 | 日/周/月视图，自动聚合全部带时间内容：定时任务、时间段任务、专注记录 |
| 打卡 | 自定义习惯打卡（次数/时长/单位/有效期），三图可视化趋势 |
| 复盘 | 日/周/月/年/五年五周期反思记录，统一五字段模板 |
| 设置 | 主题、计时参数、数据导出/导入/清空 |

## 5. 各模块第一版必要数据与操作

### 5.1 待办 Todo
- **数据**：主任务 `{id, title, note, timeType: none/date/datetime/range, startAt, endAt, completed, completedAt, tagIds[], parentId, sortOrder, createdAt}`；子任务同结构挂 parentId，层级不限（**可拆可不拆，不强制**）。
- **操作**：新建（底部弹层：标题/父任务/时间四选一精确到秒/标签/备注）、编辑、勾选完成与取消、树形展开折叠、筛选（全部/今天/逾期 + 标签/状态）、删除（二次确认；含子任务提示连带数量）。
- **规则**：逾期任务标红不锁死；某任务全部子任务完成 → 任务本体出现「一键完成」。

### 5.2 项目进度
- **数据**：项目 `{id, name, type: 学习/工作, desc, totalWorkload, workloadUnit(如小时), deadline, completed, tagIds[], createdAt}`；子任务 `{id, projectId, name, status: 未开始/进行中/已完成, dueAt, onHome, completedAt, tagIds[], sortOrder}`。
- **操作**：新建/编辑项目；总览卡片（进度条/工作量/截止倒计时/标签/类型徽章）；进入详情网格；建/改/删子任务（单独时限与状态）；勾选状态与颜色反馈（未开始灰/进行中主题色/已完成绿/逾期红）；每格子「放入首页」开关与「专注」按钮；网格/列表视图切换；全部子任务完成 → 项目可一键归档完成。

### 5.3 专注计时
- **数据**：会话 `{id, targetType: none/todo/projectSub, targetId, mode: pomodoro/free, startAt, endAt, plannedMinutes, status: 完成/中断}`。
- **操作**：从任务/子任务启动；番茄（默认专注 25min+休息 5min，设置可改）与自由正计时随时切换；开始/暂停/继续/结束；计时以开始时间戳为基准，收起浮层、切后台不丢；结束自动写入记录（不自动完成任务）；今日统计；会话历史可查。

### 5.4 标签
- **数据**：`{id, name, color}`；各实体以 tagIds 关联。
- **操作**：新建（名称+颜色）、改名、换色、删除（自动解除全部关联，二次确认）；专题页卡片墙（色块+名称+关联数）；标签详情分组列出待办任务/项目/项目子任务并可跳转；任务/子任务表单内可快捷新建标签。第一版打卡不绑标签。

### 5.5 日历
- **数据**：无独立实体，聚合读取带时间的 Todo（datetime/range）与专注会话。
- **操作**：日/周/月三视图切换；前后翻页/回到今天；日与周视图按时间轴落位（时间点=点标、时间段=横条、专注=浅色条）；月视图按天格子摘要；点空白处 → 快速新建该时刻任务；点条目 → 详情。

### 5.6 打卡
- **数据**：项目 `{id, name, unit, dailyTargetCount, fixedDurationMin(选填=时长型), startDate, endDate(null=永久), createdAt}`；记录 `{id, checkinId, count, durationMin, at}`。
- **操作**：新建（名称/单位/每日目标次数/每次固定时长选填/有效期或永久）；打卡 +1（时长型先输入本次时长）；达标口径 = 当天次数≥目标 且（时长型）总时长≥目标；历史记录列表可删单条；三图：折线（近 30/90 天次数与时长趋势）、热力（一年每日达标深浅）、甘特（每日打卡时段条）；过期项目沉底置灰，达标率/连续天数统计卡。

### 5.7 复盘
- **数据**：`{id, type: day/week/month/year/five, periodDate(周期锚点日期), fields: {plan(计划内容), events(当日/周期事件), problems(新旧问题反思), improvement(优化改善方案), summary(整体总结)}, createdAt, updatedAt}`。
- **操作**：周期分段筛选历史；新建默认定位当前周期、可改选；五字段模板表单（编辑时提供「插入本周期完成情况」快捷带入已完成任务清单，可选用）；编辑/删除。

### 5.8 设置
- **数据**：`{theme, pomodoroFocusMin, pomodoroBreakMin, dailyFocusGoalMin?}`。
- **操作**：5 套主题切换（默认 #9DE6D6+#B4204F）；计时参数；数据导出/导入/清空（见第 7 节）。

## 6. 首页、待办与其他模块的关系

数据互通与联动（实现时逐条落实）：
1. **首页 = 聚合中枢**：从各模块取摘要展示，卡片即跳转入口。
2. **待办 ↔ 日历**：任务设置时间（点/段）→ 自动落入日历对应位置；日历空白处新建即写入待办。
3. **项目子任务 ↔ 首页**：子任务开启「放入首页」→ 出现在首页今日待办，可勾选并同步回项目网格状态。
4. **专注 ↔ 各模块**：从待办任务或项目子任务启动，结束后会话记录同时进入日历（时间轴）、首页今日统计与任务/子任务的专注历史；不自动改变完成状态。
5. **标签贯穿全站**：待办任务、项目、项目子任务共享同一标签体系；标签页聚合展示并可跳回各实体。
6. **复盘 ↔ 完成记录**：写作时可一键插入周期内已完成任务清单作为素材。
7. **首页 ↔ 打卡**：今日打卡区行内快捷打卡，达标状态实时反映。
8. **完成状态联动**：子任务全完成 → 父任务可一键完成；项目子任务全完成 → 项目可归档；首页今日完成计数随之更新。

## 7. 本地数据文件备份与恢复要求

- 所有数据存于浏览器 IndexedDB 的单一全量文档（一次读写，避免多表不一致）；每次操作：内存状态即时生效 → 防抖 500ms 自动写盘。刷新/关闭/重启后完整恢复。
- **设置 → 数据** 提供：
  - **导出**：一键下载 JSON 备份文件（含全部数据与版本号）；
  - **导入**：选择备份文件导入，导入前自动把当前数据备份到独立槽位，防止误覆盖；
  - **清空**：清空全部数据（需输入确认词，执行前自动备份）。
- 适用场景：换设备/浏览器清理后导入恢复。第一版不做自动云备份与多端同步。
- 明确告知用户：数据仅存本机，浏览器清理缓存/卸载浏览器可能导致丢失，请定期导出备份。

## 8. 第一版必须完成的功能

- [ ] 全站框架：顶栏 + 底部 5 Tab 导航 + 12 路由 + 响应式布局（平板竖/横屏）。
- [ ] 待办：主任务/多级可选子任务；时间四选一（无/某日/时间点精确到秒/时间段）；勾选与逾期标红；筛选；树形管理。
- [ ] 项目：总览双视图（列表总览 + 网格详情）；项目信息（类型/内容/总工作量/截止日）；子任务单独时限与状态；「放入首页」开关；进度条与归档。
- [ ] 专注：番茄/自由双模式；全屏浮层 + 独立页；后台计时不丢；会话自动记录并联动首页/日历/详情。
- [ ] 标签：建/改/删 + 全站绑定 + 专题页与详情聚合跳转。
- [ ] 日历：日/周/月三视图自动聚合定时任务、时间段任务、专注记录；空白处快速建任务。
- [ ] 打卡：自定义项目（次数/时长/单位/有效期或永久）；打卡记录；达标判定；折线/热力/甘特三图与历史列表。
- [ ] 复盘：五周期 × 五字段模板；默认当前周期；历史筛选；插入周期完成情况。
- [ ] 首页：六大摘要卡片 + 各卡片跳转与行内快捷操作。
- [ ] 设置：5 套主题切换、计时参数、导出/导入/清空。
- [ ] PWA：可添加到主屏幕并独立全屏打开。
- [ ] 数据持久化：全部操作即时落盘，重启不丢。

## 9. 第一版暂时不做的功能

- 登录、账号、云端同步、多设备（后续再议）。
- 系统级推送通知（仅站内轻提示）。
- 复盘自动汇总代填（周复盘自动聚合日复盘等智能能力）。
- 专注记录手动补录（只能真实计时产生）。
- 全局数据统计大盘（各模块自带图表已覆盖需求）。
- 打卡项目绑定标签（保持简单）。
- 任务提醒闹铃、语音输入、协作分享等未在需求内的一切扩展。

## 10. 可实际检查的验收标准（手动走查 + 构建）

**构建与运行**
- [ ] `npm run build` 无错误通过；`npm run dev` 正常启动，12 个路由均可打开无白屏/报错。

**待办**
- [ ] 新建主任务后可不建子任务直接用；也可任意层添加子任务（3 层验证）。
- [ ] 时间设定：不设时间 / 全天某日 / 精确到秒的时间点 / 起止时间段，四种均保存正确。
- [ ] 「今天」「逾期」筛选与逾期标红正确；删除含子任务任务时有连带提示。

**项目**
- [ ] 新建学习/工作项目 → 详情为网格布局，一格一子任务，数量与格子数一致。
- [ ] 子任务独立设时限/状态，状态颜色区分正确；勾选完成 → 顶部进度条同步前进。
- [ ] 开启某子任务「放入首页」→ 首页今日待办出现该项，首页勾选后回到项目详情状态已同步。
- [ ] 全部子任务完成 → 出现「归档项目」入口，归档后总览中该项目移入已完成区。

**专注**
- [ ] 从任务与项目子任务启动浮层计时；番茄/自由模式可切换；倒计时/正计时走时准确。
- [ ] 计时中收起浮层、切后台再回，时长不丢、正确续算；结束后首页今日累计增加、日历出现该会话条、对应任务详情可见历史。

**标签**
- [ ] 新建标签 → 待办任务、项目、项目子任务均可绑定；标签详情分组列出正确并可跳转。
- [ ] 删除标签后相关实体不再显示该标签、无报错。

**日历**
- [ ] 日/周/月三视图正确渲染；时间点任务、时间段横条、专注记录落位与真实时间一致；月视图有摘要。
- [ ] 日历空白处点击 → 弹出已带时间的快速建任务，保存后出现在待办。

**打卡**
- [ ] 新建次数型与时长型项目（含永久/有效期）；打卡后当天计数/时长正确。
- [ ] 达标判定准确（次数+时长双条件），达标卡变色；连续天数正确。
- [ ] 折线/热力/甘特三图与记录数据一致（抽查最近 3 天）。

**复盘**
- [ ] 五种周期均可新建；五字段输入后重新打开内容完整回显；按周期筛选正确；可编辑/删除。

**首页**
- [ ] 各卡片数据与对应模块一致（今日待办、项目进度、专注时长、打卡、即将到期）；未写今日/本周复盘时出现提示。

**持久化与备份**
- [ ] 任意操作后刷新页面、完全关闭浏览器重开，数据完整保留。
- [ ] 导出 JSON → 执行「清空」→ 导入该文件 → 数据完整还原（条目数一致、抽查内容一致）；导入前自动备份生效。

**外观与适配**
- [ ] 5 套主题切换全局即时生效且重启后记忆所选。
- [ ] 平板竖屏全部页面可用；横屏下网格/日历/图表自动增列无错位。
- [ ] PWA：浏览器菜单「添加到主屏幕」成功，从桌面图标独立打开正常。
